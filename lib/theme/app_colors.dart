import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const accent = Color(0xFF1B4332);
  static const accentHover = Color(0xFF2D6A4F);
  static const accentLight = Color(0xFF40916C);

  // Backgrounds
  static const primaryBg = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1D1D1F);
  static const textSecondary = Color(0xFF86868B);
  static const textTertiary = Color(0xFFA1A1A6);

  // Borders
  static const borderColor = Color(0xFFD2D2D7);
  static const dividerColor = Color(0xFFE5E5E5);

  // Gradient
  static const gradientStart = Color(0xFF1B4332);
  static const gradientMid = Color(0xFF2D6A4F);
  static const gradientEnd = Color(0xFF40916C);

  // Semantic
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF2E7D32);
}
